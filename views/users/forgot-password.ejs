
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap');

        body {
            font-family: 'Poppins', sans-serif;
            background-image: url("/assets/imgs/page/back1.jpg");
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: transparent; /* Transparent container */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Subtle shadow */
            max-width: 400px;
            width: 100%;
            text-align: center;
            position: relative;
            top: 10px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .container:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
        }

        h1 {
            font-size: 24px;
            color:#110185 ;
            font-weight: 600;
            margin-bottom: 20px;
            text-transform: uppercase;
        }

        p {
            color: #110185;
            margin-bottom: 20px;
            font-size: 14px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        input[type="email"] {
            width: 100%;
            padding: 14px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 16px; /* Match font size with button */
            background: #fff;
            color: #333;
            transition: border-color 0.3s ease, background-color 0.3s ease;
            box-sizing: border-box; /* Ensure consistent sizing */
        }

        input[type="email"]::placeholder {
            color: #888;
            font-size: 16px; /* Match font size */
        }

        input[type="email"]:focus {
            border-color: #110185;
            outline: none;
            background-color: #f9f9f9;
        }

        button {
            width: 100%;
            padding: 14px; /* Same padding as input */
            border: none;
            border-radius: 8px;
            background-color: #0066b3;
            color: #fff;
            font-size: 16px; /* Match font size */
            font-weight: 600;
            cursor: pointer;
            transition: background-color 0.3s ease;
            box-sizing: border-box; /* Ensure consistent sizing */
        }

        button:hover {
            background-color: #110185;
        }

        .message {
            color: #ff4d4d;
            margin-bottom: 20px;
        }

        a {
            display: block;
            text-align: center;
            margin-top: 20px;
            text-decoration: none;
            color: #110185;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #2409a1;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Password Reset</h1>
        <form action="/forgot-password" method="POST">
            <p>Enter your email address and we'll send an OTP to reset your password.</p>
            <% if (locals.message) { %>
                <div class="message"><%= message %></div>
            <% } %>
            <input 
                type="email" 
                placeholder="Enter your Email" 
                id="typeEmail" 
                pattern="^[^\s@]+@[^\s@]+\.[^\s@]+$" 
                required 
                title="Please check your email address" 
                name="email">
            <button type="submit">Send OTP</button>
        </form>
    </div>
</body>
</html>
