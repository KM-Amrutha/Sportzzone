<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #000;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background-color:#f3eb0b;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(255, 255, 255, 0.1);
            max-width: 400px;
            width: 100%;
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .container:hover {
            transform: translateY(-10px);
            box-shadow: 0 8px 30px rgba(255, 255, 255, 0.2);
        }
        h1 {
            color: #0f0d0d;
            margin-bottom: 20px;
            font-size: 24px;
            letter-spacing: 1px;
        }
        p {
            color: #0a0808;
            margin-bottom: 20px;
            font-size: 14px;
        }
        form {
            display: flex;
            flex-direction: column;
        }
        input[type="email"] {
            padding: 12px;
            border: 1px solid #444;
            border-radius: 4px;
            margin-bottom: 20px;
            font-size: 16px;
            background: #f2f8f8;
            color: #fff;
            transition: border-color 0.3s, background-color 0.3s;
        }
        input[type="email"]:focus {
            border-color: #fff;
            outline: none;
            background-color: #444;
        }
        button {
            padding: 12px;
            border: none;
            border-radius: 4px;
            background-color: #37dd1a;
            color: #000;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
        }
        button:hover {
            background-color: #246506;
            color: #000;
        }
        .message {
            color: #ff5555;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Password Reset</h1>
        <form action="/forgot-password" method="POST">
            <p>
                Enter your email address and we'll send an OTP to reset your password.
            </p>
            <% if(locals.message) { %>
                <div class="message"><%= message %></div>
            <% } %>
            <input type="email" placeholder="Enter your Email" id="typeEmail" pattern="^[^\s@]+@[^\s@]+\.[^\s@]+$" required title="Please check your email address" name="email">
            <button type="submit">Send OTP</button>
        </form>
    </div>
</body>
</html>
